
import ErrorIcon from "./icons/Error";
import Valid from "./icons/Valid";
export default function Input(props) {
    return(
        <div
        className={'input-group ${
            !props.isValid && props.isTouched
            ?"invali"
            :'${props.isValid &&propS.isTouched ? "vali":""}'}'} >
            
            
            
            
            
            
            
            </div>
    )
}